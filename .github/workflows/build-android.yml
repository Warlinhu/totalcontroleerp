name: Build Android APK

# Gera o .apk automaticamente na nuvem toda vez que você faz push
# na branch main (ou dispara manualmente pela aba Actions do GitHub).
# O arquivo pronto fica disponível como "artifact" para download.

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

env:
  NODE_VERSION: "22"

jobs:
  build:
    runs-on: ubuntu-latest
    concurrency:
      group: totalcontroleerp-latest-release
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Setup Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v4

      - name: Install dependencies
        run: npm ci

      - name: Prepare Android web assets
        shell: bash
        run: |
          mkdir -p dist
          cat > dist/index.html <<'HTML'
          <!doctype html>
          <html lang="pt-BR">
            <head>
              <meta charset="utf-8" />
              <meta name="viewport" content="width=device-width, initial-scale=1" />
              <title>TotalControle ERP</title>
            </head>
            <body>Carregando TotalControle ERP…</body>
          </html>
          HTML

      - name: Add Android platform
        shell: bash
        run: |
          if [ ! -d android ]; then
            npx cap add android
          fi
          npx cap sync android

      - name: Make Gradle executable
        run: chmod +x android/gradlew

      - name: Build debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Rename APK
        run: cp android/app/build/outputs/apk/debug/app-debug.apk TotalControleERP.apk

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: TotalControleERP-android-debug
          path: TotalControleERP.apk
          if-no-files-found: error
          retention-days: 30

      - name: Publish to GitHub Release (latest)
        uses: softprops/action-gh-release@v3
        with:
          tag_name: latest
          name: Latest build
          make_latest: "true"
          overwrite_files: true
          files: TotalControleERP.apk

